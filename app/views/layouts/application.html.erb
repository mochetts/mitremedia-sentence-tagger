<!DOCTYPE html>
<html>
  <head>
    <title>MitremediaTextTagger</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class="container mx-auto mt-20 px-3 sm:px-5 flex flex-col">
      <% flash.each do |type, msg| %>
        <p class="py-2 px-3 <%= type == 'error' ? 'bg-rose-700' : 'bg-emerald-500' %> mb-5 text-white font-medium fixed w-screen top-0 left-0">
          <%= msg %>
        </p>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
